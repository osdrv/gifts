<% form_for :gift, @new_gift, :url => { :action => "add_wish" }, :html => { :id => "add_wish_form" } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit " + " %>
<% end %>

<script type='text/javascript'>
  add_wish();
</script>